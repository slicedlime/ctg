tellraw @a [{"text":"\n"},{"translate":"Place a command block on the emerald block. Make it clone only the diamond blocks to on top of the command block itself, regardless of where that command block is placed.", "color":"gold", "with": [{"translate": "clone only the diamond blocks to on top of the command block itself", "color": "green"}]}]
